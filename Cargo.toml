[package]
name = "catbath"
version = "0.1.2"
edition = "2024"
description = "a tiny terminal text editor with search, undo, mouse."

[dependencies]
crossterm = "0.28"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
