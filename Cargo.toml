[package]
name = "catbath"
version = "0.1.3"
edition = "2024"
description = "a tiny editor: terminal-first, browser-curious, extension-friendly."

[dependencies]
crossterm = "0.28"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
